import 'package:flutter/material.dart';

class WeatherForecaseModal extends StatelessWidget {
  const WeatherForecaseModal({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
